{
  "name": "personal-music-mix",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "test": "node --test tests/*.cjs tests/*.mjs",
    "db:generate": "drizzle-kit generate"
  },
  "devDependencies": {
    "@openai/sites-vite-plugin": "0.2.0",
    "vite": "^8.0.0",
    "drizzle-kit": "^0.31.0",
    "drizzle-orm": "^0.44.0"
  }
}
